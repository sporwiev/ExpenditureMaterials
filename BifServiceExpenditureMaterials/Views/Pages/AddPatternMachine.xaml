<UserControl x:Class="BifServiceExpenditureMaterials.Views.Pages.AddPatternMachine"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BifServiceExpenditureMaterials.Views.Pages" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          
        </Grid.RowDefinitions>
        <TextBlock Text="Добавить новое правило для машин" FontSize="20" TextWrapping="Wrap" Width="250" TextAlignment="Left" HorizontalAlignment="Left"/>
        <StackPanel Margin="10" Grid.Row="1">
            <ui:InfoBar x:Name="infobar" Title="FSDFDSFDSF" Message="DSFSDFDSF" Opacity="0" IsClosable="False" IsOpen="True" Severity="Success"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10">
            <ComboBox Width="200" x:Name="listmachine"/>
            <ui:TextBox PlaceholderText="*С825МР198*" Margin="10,0,0,0" x:Name="machineeditorpattern"/>
            <ui:Button VerticalAlignment="Stretch" Width="Auto" Content="Добавить правило" Margin="10,0,0,0" Click="AddNewPattern_Click"/>
        </StackPanel>
        <ScrollViewer Grid.Row="3" Margin="10">
            <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="10">
                <ui:ListView x:Name="listnamemachine" Width="Auto" ScrollViewer.VerticalScrollBarVisibility="Disabled"/>
                <ui:ListView x:Name="listpatternmachine" Width="Auto" ScrollViewer.VerticalScrollBarVisibility="Disabled"/>
                <ui:ListView x:Name="listbuttonsubmit" Width="Auto" ScrollViewer.VerticalScrollBarVisibility="Disabled"/>
                <ui:ListView x:Name="listbuttondelete" Width="Auto" ScrollViewer.VerticalScrollBarVisibility="Disabled"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
