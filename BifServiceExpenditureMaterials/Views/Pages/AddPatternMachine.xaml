<UserControl x:Class="BifServiceExpenditureMaterials.Views.Pages.AddPatternMachine"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Правила распознавания машин"
                   FontSize="22" FontWeight="SemiBold"
                   Grid.Row="0"/>

        <!-- Форма добавления -->
        <ui:Card Grid.Row="2" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Машина" FontWeight="Medium" Margin="0,0,0,4"/>
                    <ComboBox x:Name="listmachine"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Шаблон (например: *С825МР198*)" FontWeight="Medium" Margin="0,0,0,4"/>
                    <ui:TextBox x:Name="machineeditorpattern"
                                PlaceholderText="*С825МР198*"/>
                </StackPanel>

                <ui:Button Grid.Column="4"
                           Content="Добавить правило"
                           Icon="{ui:SymbolIcon Add24}"
                           Appearance="Primary"
                           VerticalAlignment="Bottom"
                           Click="AddNewPattern_Click"/>
            </Grid>
        </ui:Card>

        <!-- Таблица правил -->
        <ui:Card Grid.Row="4" Padding="0">
            <DataGrid x:Name="patternsGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      AlternatingRowBackground="#F7F9FC"
                      BorderThickness="0"
                      RowHeight="38">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#EDF2F7"/>
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Машина" Binding="{Binding Name}" Width="200" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Шаблон" Binding="{Binding Pattern}" Width="*"/>
                    <DataGridTemplateColumn Header="Действия" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <ui:Button Content="Сохранить"
                                               Margin="0,0,6,0"
                                               Padding="8,4"
                                               Click="SavePattern_Click"
                                               Tag="{Binding Id}"/>
                                    <ui:Button Content="❌"
                                               Padding="8,4"
                                               Appearance="Danger"
                                               Click="DeletePattern_Click"
                                               Tag="{Binding Id}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ui:Card>

    </Grid>
</UserControl>
