<UserControl x:Class="BifServiceExpenditureMaterials.Views.Pages.BayMaterialsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BifServiceExpenditureMaterials.Views.Pages"
             xmlns:controls="clr-namespace:BifServiceExpenditureMaterials.Controls"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <GroupBox Header="Заправка материалов" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Выберите тип" Margin="0,10,0,10"/>
                <ComboBox SelectionChanged="ComboBox_SelectionChanged" x:Name="TypeProductComboBox" Background="LightGray">
                    <ComboBoxItem Content="Масло"/>
                    <ComboBoxItem Content="Антифриз"/>
                    <ComboBoxItem Content="Смазка"/>
                    <ComboBoxItem Content="Фильтр"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Row="2">
                <TextBlock Text="Выберите продукт" Margin="0,10,0,0"/>
                <ComboBox x:Name="ComboBoxProduct" Margin="0,10,0,10" SelectionChanged="ComboBoxProduct_SelectionChanged" Background="LightGray">
                </ComboBox>
                <TextBlock Text="Остаток" Margin="0,10,0,10"/>
                
            </StackPanel>
            <StackPanel Grid.Row="3">
                <TextBlock x:Name="CountTextBox" Text="Текущий остаток: " Background="LightGray" Padding="8" Margin="0,0,0,10"/>
                <TextBlock Text="Заправить продукт (Пример: 100,200,300) расчет в литрах или штуках" Margin="0,0,0,10"/>
                <ui:TextBox x:Name="TextBoxCountProduct" PlaceholderText="200" Grid.Row="5" Grid.Column="0"/>
                <Button Content="Заправить" Click="Button_Click"/>
            </StackPanel>



        </Grid>

    </GroupBox>
</UserControl>
