<UserControl x:Class="BifServiceExpenditureMaterials.Views.Pages.ViewMachinePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:BifServiceExpenditureMaterials.Views.Pages"
    xmlns:controls="clr-namespace:BifServiceExpenditureMaterials.Controls"
   
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d" 

    d:DesignHeight="450" d:DesignWidth="800">   
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <StackPanel>
                <TextBlock Text="Машина" Margin="0,0,0,5"/>
                <ComboBox Background="LightGray" Name="MachineComboBox" Width="200" SelectedIndex="0" SelectionChanged="MachineComboBox_SelectionChanged"/>
            </StackPanel>
            <StackPanel Margin="10,0,0,0">
                <TextBlock Text="Месяц" Margin="0,0,0,5"/>
                <ComboBox Background="LightGray" Name="MounthComboBox" Width="200" SelectedIndex="0" SelectionChanged="MachineComboBox_SelectionChanged"/>
            </StackPanel>
            <StackPanel Margin="10,0,0,0">
                <TextBlock Text="Год" Margin="0,0,0,5"/>
                <ComboBox Background="LightGray" Name="YearComboBox" Width="200" SelectedIndex="0" SelectionChanged="MachineComboBox_SelectionChanged"/>
            </StackPanel>
        </StackPanel>
         <DataGrid Grid.Row="1" x:Name="datagrid" Margin="0,10,0,0">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Opacity" Value="0" />
                    <Style.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                Storyboard.TargetProperty="Opacity"
                                From="0" To="1" Duration="0:0:1.0" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
       
    </Grid>
</UserControl>
