<UserControl x:Class="BifServiceExpenditureMaterials.Controls.Charts"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="200" MinWidth="300">
    <Border  Padding="10" Background="White">
        <Grid>
            <Canvas x:Name="ChartCanvas" Margin="30,0,0,0">
                <Canvas.RenderTransform>
                    <ScaleTransform x:Name="CanvasScaleTransform"/>
                </Canvas.RenderTransform>
                <Canvas/>
            </Canvas>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.RowSpan="6" x:Name="panelindexY" Margin="0,10,0,0">
                    <TextBlock FontSize="10" Margin="3,3,3,7" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <TextBlock FontSize="10" Margin="3,3,3,7" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <TextBlock FontSize="10" Margin="3,3,3,7" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <TextBlock FontSize="10" Margin="3,3,3,7" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <TextBlock FontSize="10" Margin="3,3,3,7" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <TextBlock FontSize="10" Margin="3,3,3,7" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <TextBlock FontSize="10" Margin="3,3,3,7" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </StackPanel>
               
                <Border Grid.RowSpan="7" BorderThickness="1,0,0,1" BorderBrush="Gray" Grid.Column="1"/>
                <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" Background="Gray" Height="1" IsEnabled="False" Grid.Column="1"/>
                <GridSplitter Grid.Row="3" HorizontalAlignment="Stretch" Background="Gray" Height="1" IsEnabled="False" Grid.Column="1"/>
                <GridSplitter Grid.Row="5" HorizontalAlignment="Stretch" Background="Gray" Height="1" IsEnabled="False" Grid.Column="1"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
